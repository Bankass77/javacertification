package org.example.algortihme.interview.designpaterns.strategy;

/**
 * Represents a text formatter with a format method
 */
public interface TextFormatter {
    // Format a text an d return void
    void format(String text);
}
